[project]
name = "fintech_alpha"
requires-python = ">=3.10,<3.15"
version = "0.1.0"
dependencies = [
    "dagster==1.12.10",
    "dagster-cloud",
    "dagster-snowflake",
    "dagster-dbt",
    "dagster-databricks",
    "pandas",
    "snowflake-connector-python",
]

[dependency-groups]
dev = [
    "dagster-webserver",
    "dagster-dg-cli",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
force-include = { "pyproject.toml" = "pyproject.toml" }

[tool.dg]
directory_type = "project"

[tool.dg.project]
root_module = "fintech_alpha"
registry_modules = [
    "fintech_alpha.components.*",
]
